namespace CKL.Libs.SiteBuilder;

/// <summary>
/// Placeholder public surface for the SiteBuilder package, so the CLI
/// (CKL.Apps.SiteBuilder) has a referencable type before the real pipeline
/// API lands in plan 0013.
/// </summary>
public static class SiteBuilderInfo
{
    /// <summary>The package's current version.</summary>
    public const string Version = "1.0.0";
}
